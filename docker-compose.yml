services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: omnilith
      POSTGRES_PASSWORD: omnilith
      POSTGRES_DB: omnilith
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
