{
  "name": "omnilith",
  "private": true,
  "scripts": {
    "architecture-report": "node scripts/architecture-report.mjs",
    "web:dependency-graph": "node scripts/generate-web-dependency-graph.mjs",
    "web:dependency-graph:docs": "node scripts/generate-web-dependency-graph.mjs --out docs/web-dependency-graph.html",
    "boundaries": "node scripts/check-boundaries.mjs",
    "intent-headers": "node scripts/check-intent-headers.mjs",
    "intent-headers:strict": "node scripts/check-intent-headers.mjs --strict",
    "intent-headers:update-baseline": "node scripts/check-intent-headers.mjs --update-baseline",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "check": "pnpm run boundaries && pnpm run intent-headers && pnpm -r build && pnpm -r lint && pnpm -r test",
    "format": "biome format --write packages/",
    "dev": "pnpm --filter @omnilith/api --filter @omnilith/web --parallel dev",
    "dev:unified": "DATABASE_URL=postgres://localhost:5432/omnilith_dev PORT=3000 OMNILITH_SEED_PROFILE=v1-demo VITE_AUTH_ENABLED=true VITE_GUEST_ACCESS_STRATEGY=interest VITE_API_TARGET=http://localhost:3000 VITE_DEV_PORT=5173 pnpm --filter @omnilith/api --filter @omnilith/web --parallel dev",
    "dev:demo": "pnpm run dev:unified",
    "dev:auth": "pnpm run dev:unified",
    "db:migrate": "pnpm --filter @omnilith/api db:migrate",
    "db:reset": "OMNILITH_ALLOW_DB_RESET=true pnpm --filter @omnilith/api db:reset",
    "db:reset:unified": "OMNILITH_ALLOW_DB_RESET=true DATABASE_URL=postgres://localhost:5432/omnilith_dev OMNILITH_SEED_PROFILE=v1-demo pnpm --filter @omnilith/api db:reset",
    "db:reset:demo": "pnpm run db:reset:unified",
    "db:reset:auth": "pnpm run db:reset:unified",
    "hooks:install": "git config core.hooksPath .githooks",
    "hooks:pre-commit": "pnpm -r lint",
    "hooks:pre-push": "pnpm run check",
    "issue-proposals:validate": "node scripts/issue-proposals.mjs validate",
    "issue-proposals:sync": "node scripts/issue-proposals.mjs sync",
    "agent:receiver": "node scripts/agent-webhook-receiver.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/node": "^25.2.3"
  }
}
