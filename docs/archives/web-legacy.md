# Legacy Web Archive

Legacy `packages/web` was archived before removal from `main`.

- Archive branch: `archive/web-legacy`
- Archive tag: `archive/web-legacy-2026-02-24`
- Merge commit before removal: `3efa8aa23abaa3cdb8a600f0fb06b4e285d69594`

To restore the legacy package into a working branch:

```bash
git checkout archive/web-legacy-2026-02-24 -- packages/web
```
